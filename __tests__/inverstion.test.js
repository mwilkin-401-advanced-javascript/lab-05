'use strict';

jest.mock('fs');

const doTheInversion = require('../lib/inversion.js');

describe('Inversion transformation module', () => {
  it('will invert the colors of a bitmap', () =>{

  });
});

'use strict';

jest.mock('fs');

const darkenImage = require('../lib/darken.js');

describe('Darken transformation module', () => {
  it('will darken the coloration of a bitmap', () =>{
    
  });
});
'use strict';

jest.mock('fs');

const transformGreyscale = require('../lib/grayscale.js');

describe('Grayscale transformation module', () => {
  it('will transform a bitmap to grayscale', () =>{
    
  });
});
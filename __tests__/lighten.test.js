'use strict';

jest.mock('fs');

const lightenImage = require('../lib/lighten.js');

describe('Lighten transformation module', () => {
  it('will lighten the coloration of a bitmap', () =>{
    
  });
});